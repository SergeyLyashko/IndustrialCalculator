package calculators;

import org.junit.Before;
import org.junit.Test;

import static org.junit.Assert.*;

public class SquareSteelMassCalculatorTest {
/*
    private Detail detail;
    @Before
    public void setUp() throws Exception {
        detail = new Detail(){
            @Override
            public double getFieldsValue() {
                return 3.45;
            }

            @Override
            public double getDataBaseValue() {
                return 22.7;
            }
        };
    }

    @Test
    public void calculationMass() {
        double dataBaseValue = detail.getDataBaseValue();
        double fieldsValue = detail.getFieldsValue();
        assertEquals(dataBaseValue*fieldsValue, 22.7*3.45, 0.1);
    }

 */
}